<?xml version="1.0" encoding="utf-8"?>
<paths>
    <cache-path name="share" path="share/" />
    <!-- In-app updater APK downloads (filesDir root, update-*.apk). -->
    <files-path name="updates" path="." />
</paths>
